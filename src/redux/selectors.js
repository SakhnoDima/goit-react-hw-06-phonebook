export const getContacts = state => state.contacts;

export const getFilter = state => state.filter.value;

export const getModalShow = state => state.modalShow.onShow;

export const getTheme = state => state.theme.them;
